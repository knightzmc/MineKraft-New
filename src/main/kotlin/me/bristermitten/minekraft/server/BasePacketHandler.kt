package me.bristermitten.minekraft.server

import com.soywiz.korio.net.AsyncClient

class BasePacketHandler  : (AsyncClient) -> Unit{
    override fun invoke(p1: AsyncClient) {
        TODO("Not yet implemented")
    }
}
